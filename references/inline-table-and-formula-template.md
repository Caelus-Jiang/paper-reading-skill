# 就地表格与公式规范

## 目录

- 主张与实验表
- 公式解释
- 数学格式
- 图片定位

## 主张与实验表

### 主张—证据表（1.3）

| 主张 ID | 主张内容 | 原文位置 | 证据类型 | 证据强度 | Reviewer 结论 |
|---|---|---|---|---|---|
| C1 |  |  | 主实验 / 理论 / 消融 | 强 / 部分 / 间接 / 不足 |  |

### 实验—主张矩阵（3.1）

| 实验组 | 对应主张 | 是否充分验证 | 缺失项 |
|---|---|---|---|
|  |  |  |  |

### 主结果表（3.3）

| 设置 / 数据集 | 方法 | 指标 | 数值 | 审稿人提示 |
|---|---|---|---|---|
|  |  |  |  |  |

### 关键消融表（3.3 或 3.4）

| 消融项 | 变化设置 | 指标 | 原始数值 | 变化后数值 | 审稿人提示 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### 相关论文补充表（4.5）

| 论文标题 | arXiv ID | 作者 / 年份 | 来源 / 类型 | 与原论文关系 | 一句话概述 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

不要在本表中添加 arXiv、DOI、项目页或其他网络链接。保留基础 arXiv ID，供同步脚本通过 Obsidian 阅读报告索引做精确匹配；同步时只有唯一命中才把标题改写为 `[[{arxiv_id}_阅读报告\|论文标题]]`，未命中或命中歧义时保持纯文本。外部核查链接统一放在附录 B。

## 公式解释

在公式首次成为关键论据的位置附近解释：

- 该公式负责什么
- 符号和每一项的含义
- 自然语言直译
- 训练/推理中的实现映射
- 依赖的假设
- 删除或替换该项会怎样
- 必要时给极简数值例子

公式解释跟着上下文出现，不集中放到单独“公式卡”章节。

## 数学格式

行内公式：`$\mathcal{L}$`。

单行 display：

```markdown
$$ \mathcal{L} = \mathcal{L}_{\mathrm{task}} + \lambda \mathcal{L}_{\mathrm{aux}} \tag{1} $$
```

多行 display：

```markdown
$$
\begin{aligned}
\mathcal{L} ={}& \mathcal{L}_{\mathrm{task}} \\
&+ \lambda \mathcal{L}_{\mathrm{aux}} \tag{2}
\end{aligned}
$$
```

规则：

- 只有需要正文引用的关键公式才编号。
- 编号从 1 连续递增，正文写“式 (1)”。
- 多行必须使用 `aligned`、`gathered`、`split` 等标准环境。
- 不要在 display 中裸写以 `+`、`-`、`*` 开头的 Markdown 行。
- 不要用反引号包装数学变量或公式。
- 代码名可以使用反引号，例如函数 `cross_entropy` 对应损失 $\mathcal{L}_{\mathrm{CE}}$。

## 图片定位

使用 `images_manifest.json` 中的以下字段定位：

- `graphics_target`
- `caption`
- `label`
- `section_hint`
- `first_reference_hint`
- `source_tex`

优先用源码中的作者原图并与 PDF 核对；源码不可用时依次退回 PDF 嵌入图片、figure 裁剪和整页 fallback。禁止用网页截图交付。
